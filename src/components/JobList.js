import React from "react";
import PropTypes from "prop-types";
import { makeStyles } from "@material-ui/core/styles";
import Tabs from "@material-ui/core/Tabs";
import Tab from "@material-ui/core/Tab";
import Typography from "@material-ui/core/Typography";
import Box from "@material-ui/core/Box";
import FadeInSection from "./FadeInSection";

const isHorizontal = window.innerWidth < 600;

function TabPanel(props) {
  const { children, value, index, ...other } = props;

  if (isHorizontal) {
    return (
      <div
        role="tabpanel"
        hidden={value !== index}
        id={`full-width-tabpanel-${index}`}
        aria-labelledby={`full-width-tab-${index}`}
        {...other}
      >
        {value === index && (
          <Box p={3}>
            <Typography>{children}</Typography>
          </Box>
        )}
      </div>
    );
  } else {
    return (
      <div
        role="tabpanel"
        hidden={value !== index}
        id={`vertical-tabpanel`}
        {...other}
      >
        {value === index && (
          <Box p={3}>
            <Typography>{children}</Typography>
          </Box>
        )}
      </div>
    );
  }
}

TabPanel.propTypes = {
  children: PropTypes.node,
  index: PropTypes.any.isRequired,
  value: PropTypes.any.isRequired
};

function a11yProps(index) {
  if (isHorizontal) {
    return {
      id: `full-width-tab-${index}`,
      "aria-controls": `full-width-tabpanel-${index}`
    };
  } else {
    return {
      id: `vertical-tab-${index}`
    };
  }
}

const useStyles = makeStyles(theme => ({
  root: {
    flexGrow: 1,
    backgroundColor: "theme.palette.background.paper",
    display: "flex",
    height: 300
  },
  tabs: {
    borderRight: `1px solid ${theme.palette.divider}`
  }
}));

const JobList = () => {
  const classes = useStyles();
  const [value, setValue] = React.useState(0);

  const experienceItems = {
    Nodel: {
      jobTitle: "Data Scientist |",
      duration: "NOV 2021 - PRESENT",
      desc: [
        "Work in multiple projects related with social media information using machine learning models and NLP to find insights for the clients.",
      ]
    },
    Telconet: {
      jobTitle: "Data Scientist and Artificial Intelligence Engineer |",
      duration: "JUN 2021 - NOV 2021",
      desc: [
        "Work implementing Machine Learning Models (Regressive models, LSTM, ARIMA, etc) to do sales forecasting for Fast Food CompanY.",
      ]
    },
    "ESPOL Polytechnic University": {
      jobTitle: "Research Assistant |",
      duration: "SEP 2020 - FEB 2021",
      desc: [
        "The RAP system: automatic feedback of oral presentation skills using multimodal analysis and low-cost sensors. Researched and implemented machine learning models to improve accuracy of the project",
      ]
    },
    "Freelancer": {
      jobTitle: "DS/AI/ML Engineer |",
      duration: "2019 - 2020",
      desc: [
        "Recognition and classification of inorganic waste as plastic, glass, metal and paper using CNN to help Guayaquil Municipality, Ecuador to automate the process of recycle.",
        "Performed Multivariate Time Series Analysis and Benchmarking of regression models (Decision Tree, SVM, LSTM) to predict the amount of food to be supplied in a shrimp pool and reduce its waste.",
        "Using Retail Store transactions to implemented geospatial analysis to propose transaction metrics and constructed affected and non-affected zones after an earthquake."
      ]
    }
  };

  const handleChange = (event, newValue) => {
    setValue(newValue);
  };

  return (
    <div className={classes.root}>
      <Tabs
        orientation={!isHorizontal ? "vertical" : null}
        variant={isHorizontal ? "fullWidth" : "scrollable"}
        value={value}
        onChange={handleChange}
        className={classes.tabs}
      >
        {Object.keys(experienceItems).map((key, i) => (
          <Tab label={isHorizontal ? `0${i}.` : key} {...a11yProps(i)} />
        ))}
      </Tabs>
      {Object.keys(experienceItems).map((key, i) => (
        <TabPanel value={value} index={i}>
          <span className="joblist-job-title">
            {experienceItems[key]["jobTitle"] + " "}
          </span>
          <span className="joblist-job-company">{key}</span>
          <div className="joblist-duration">
            {experienceItems[key]["duration"]}
          </div>
          <ul className="job-description">
            {experienceItems[key]["desc"].map(function (descItem, i) {
              return (
                <FadeInSection delay={`${i + 1}00ms`}>
                  <li key={i}>{descItem}</li>
                </FadeInSection>
              );
            })}
          </ul>
        </TabPanel>
      ))}
    </div>
  );
};

export default JobList;
